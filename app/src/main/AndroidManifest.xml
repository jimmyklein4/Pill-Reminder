<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jimmyklein.pillreminder" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".DoctorPatientActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PatientLogIn"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".DoctorInformation"
            android:label="Doctor Information" >

        </activity>

        <activity
            android:name=".DoctorView"
            android:label="Doctor Page" >

        </activity>


        <activity
            android:name=".DoctorViewAddPatient"
            android:label="Add Patient" >

        </activity>
    </application>

</manifest>
